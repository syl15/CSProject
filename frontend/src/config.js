// Comment out whichever you want to use. Only one can be commented out at a time
//export const BASE_URL = import.meta.env.VITE_BASE_URL;  // LIVE URL
 export const BASE_URL = import.meta.env.VITE_LOCAL_BASE_URL; // LOCAL URL